import { AssistantOuAssiste } from './assistant-ou-assiste';

describe('AssistantOuAssiste', () => {
  it('should create an instance', () => {
    expect(new AssistantOuAssiste()).toBeTruthy();
  });
});
